import Store from '../stroe';
import * as Utils from 'engine/utils';

class CompositionParser{
    constructor(){

    }

    checkNote(trackIndex, start, end){
        track = Utils.getTrackByIndex(strore.getState().tracks.trackList, trackIndex);
        //TODO
    }

}

export default CompositionParser;