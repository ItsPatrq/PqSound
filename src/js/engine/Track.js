class Track {
    constructor(name, instrument, volume, mute, solo, index) {
      this.name = name;
      this.instrument = instrument;
      this.volume = volume;
      this.mute = mute;
      this.solo = solo;
      this.index = index;
    }
  
  }
  
  export default Track;